{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1 npx prisma migrate deploy; fi && npx prisma generate && npx tsx scripts/fetch-hledger.ts && npx next build",
  "crons": [
    {
      "path": "/api/cron/snapshot-metadata",
      "schedule": "0 9 * * *"
    },
    {
      "path": "/api/cron/verify-ledger",
      "schedule": "0 10 * * *"
    }
  ]
}
